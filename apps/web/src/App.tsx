export default function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="eyebrow">UniSupport</p>
        <h1>University IT help desk starter</h1>
        <p>
          This workspace now has a real starting point: a React frontend, a Nest backend,
          and shared workspace packages.
        </p>
      </section>
    </main>
  );
}
